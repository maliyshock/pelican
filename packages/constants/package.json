{
  "name": "@pelican/constants",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.d.ts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && tsc-alias -p tsconfig.json",
    "types:clean": "[ -d ./src/types/build ] && rm -r ./src/types/build || echo 'Directory does not exist'",
    "types:build": "pnpm run types:clean && npx tsx processNodes.ts ",
    "barrels:generate": "barrelsby -D -d ./src -e 'processNodes' && barrelsby -D -L -d ./src/types"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pelican/utils": "workspace:^",
    "lodash": "^4.17.21",
    "reactflow": "^11.11.3"
  },
  "devDependencies": {
    "@types/lodash": "^4.17.5",
    "@types/node": "^20.12.7",
    "@types/react": "^18.2.79",
    "barrelsby": "^2.8.1",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.4.5"
  }
}
